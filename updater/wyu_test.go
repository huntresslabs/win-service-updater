package updater

import (
	"fmt"
	"testing"
)

func TestWYU(t *testing.T) {
	err := fmt.Errorf("not tested")
	t.Fatal(err)

	// src := "some.wyu"
	// dst := "somepath"
	// _, _, err := Unzip(src, dst)
	// assert.Nil(t, err)
}
